const capitalize = str => str[0].toUpperCase() + str.slice(1).toLowerCase()
    
const bob = capitalize("bOb") // returns Bob
console.log(b)
const taylor = capitalize("TAYLOR") // returns Taylor
console.log(t)
const felishia = capitalize("feliSHIA") // returns Felishia
console.log(f)